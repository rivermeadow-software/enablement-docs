# Network Isolation
The RiverMeadow platform enables accelerated migration testing with no source impact through the use of network isolation for the target or clone instance. The migrated server must have a routable IP address that is different than the IP address of the source server.

### Dedicated Migration Network

A dedicated migration network